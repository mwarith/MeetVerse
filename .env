# Frontend Environment Variables

# Development - local signaling server
VITE_SIGNALING_SERVER_URL=http://localhost:5000

# For production deployment on same server, leave this empty or don't set it
# The app will automatically connect to the same origin
